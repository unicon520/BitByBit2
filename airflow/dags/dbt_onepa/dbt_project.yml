name: 'onepa_elt'
version: '1.0.0'
config-version: 2

profile: 'onepa_elt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  onepa_elt:
    staging:
      +materialized: view
    marts:
      +materialized: table
    embeddings:
      +materialized: incremental
